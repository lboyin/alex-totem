name: Build ZMK firmware

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    # Pinned to v0.3 (last release before Zephyr 4.1 migration that broke
    # deep-sleep wake — see zmkfirmware/zmk#3207, #3195).
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.3
